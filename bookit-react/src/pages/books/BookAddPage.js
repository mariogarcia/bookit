import React from 'react'
import { Page, Actions, Content } from '../../components/page'

import './BookListPage.css'

/**
 * Adds a new book
 *
 * @since 0.1.0
 */
export const BookAddPage = (props) => (
    <Page title='Books: Add'>
        <Content>
            Adds new book
        </Content>
    </Page>
)
